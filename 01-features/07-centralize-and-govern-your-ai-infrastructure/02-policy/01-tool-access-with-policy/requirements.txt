# Core AWS SDK
boto3>=1.42.0
botocore>=1.34.0

# AgentCore native SDK
bedrock-agentcore>=1.5.0

# AgentCore starter toolkit — used for GatewayClient (OAuth + MCP gateway setup)
# and PolicyClient (NL2Cedar policy generation API wrapper)
bedrock-agentcore-starter-toolkit>=0.2.4

# Strands agent framework
strands-agents>=0.1.0
strands-agents-tools>=0.1.0

# HTTP requests (OAuth token exchange + Gateway JSON-RPC calls)
requests>=2.31.0
