# Bounds mirror pyproject.toml. The Dockerfile installs from THIS file, so the
# upper bounds must live here to keep the deployed container reproducible.
aws-opentelemetry-distro>=0.8.0,<1.0
bedrock-agentcore>=1.9.1,<2.0
boto3>=1.35.0,<2.0          # Textract AnalyzeExpense (OCR) + DynamoDB; pulls botocore
botocore[crt]>=1.35.0,<2.0
mcp>=1.19.0,<2.0            # MCP transport to the Gateway
strands-agents>=1.13.0,<2.0
