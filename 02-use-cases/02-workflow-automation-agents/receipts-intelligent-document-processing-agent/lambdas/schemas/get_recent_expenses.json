[{"name": "get_recent_expenses", "description": "List a user's most recent expenses (newest first) so you can detect a likely duplicate before saving a new one.", "inputSchema": {"type": "object", "properties": {"user_id": {"type": "string", "description": "The end-user id whose expenses to list"}, "limit": {"type": "integer", "description": "Max number of recent expenses to return (default 20, max 100)"}}, "required": ["user_id"]}}]
