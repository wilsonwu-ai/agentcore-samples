[{"name": "lookup_merchant", "description": "Normalize a raw merchant name from a receipt against the merchant catalog. Returns the canonical merchant if known, otherwise a cleaned passthrough you can still use.", "inputSchema": {"type": "object", "properties": {"name": {"type": "string", "description": "The raw merchant name as it appears on the receipt"}}, "required": ["name"]}}]
